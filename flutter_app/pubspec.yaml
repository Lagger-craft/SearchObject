name: search_object
description: SearchObject - Organizador de objetos con fotos y búsqueda
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter

  # Bridge HTTP (dev) / MethodChannel (prod)
  http: ^1.2.0
  path_provider: ^2.1.0

  # Persistencia local
  shared_preferences: ^2.3.0

  # Cámara y galería
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
